# 1.3. Реализовать сценарий Web-MVP — подробный план

Этот документ детализирует пункт 1.3 из [основного плана](PLAN.MD). Он
превращает подготовленную в этапе 1.2 основу в первый законченный
пользовательский сценарий: человек регистрируется, создаёт проект ремонта,
добавляет комнаты и покупки, затем видит расходы и остаток бюджета.

Перед началом прочитайте [контракт API](API.MD): он уже фиксирует маршруты,
JSON-форматы, JWT, статусы ответов и правила владения. Если при реализации
нужен другой контракт, сначала согласованно измените `API.MD`, а потом код,
тесты и frontend.

## Результат этапа

Web-приложение позволяет пользователю:

1. зарегистрироваться и войти;
2. создать и выбрать свой проект;
3. настроить тип объекта, адрес и плановый бюджет;
4. создать комнаты;
5. создать, отредактировать, отфильтровать и удалить покупки;
6. увидеть обзор проекта, расходы по категориям и остаток бюджета.

Каждый ресурс принадлежит текущему пользователю. Данные другого аккаунта не
видны через API и недоступны из интерфейса. Backend, frontend, миграции и
автоматические проверки остаются согласованы.

## Принципы работы

- Реализуем вертикальными срезами: сначала модель, миграция, API и тесты
  одного ресурса, затем экран, который использует этот API.
- Не меняем уже применённые миграции. Любое изменение схемы — новая миграция
  Alembic.
- Деньги передаём только десятичными строками и обрабатываем через
  `Decimal`/`NUMERIC`; не используем `float`.
- Хешируем пароль готовой библиотекой. Исходный пароль, `password_hash`, JWT и
  секрет подписи не попадают в логи, ответы API или Git.
- Защита доступа выполняется в backend. Скрыть кнопку в React недостаточно.
- Каждый новый API-маршрут сначала покрываем backend-тестом; затем подключаем
  к нему frontend.
- После каждого небольшого среза запускаем нужные проверки, а не откладываем
  их до конца этапа.

## 0. Что уже готово

- [x] Существует таблица `accounts` с UUID, email, хешем пароля, именем и
  временем создания.
- [x] Миграция для имени `accounts.name` уже создана и применена.
- [x] Созданы SQLAlchemy Base, engine/session, Alembic и отдельная тестовая БД.
- [x] Описан контракт ресурсов `auth`, `profile`, `projects`, `rooms`,
  `purchases`, `overview` и `budget` в [API.MD](API.MD).
- [x] CI запускает линтеры, unit- и интеграционные тесты, проверку миграций,
  frontend lint и production build.

Не переписывайте эти части заново. В этом этапе мы расширяем их.

## 1. Зафиксировать границы первого пользовательского сценария

- [ ] Перед реализацией перечитать разделы 4–6 [API.MD](API.MD) и убедиться,
  что маршруты, владение ресурсами, поля и правила денег понятны.
- [ ] Работать только с ресурсами Web-MVP: аккаунт, профиль, проект, комната,
  покупка, обзор и бюджет.
- [ ] Не добавлять в этот этап фотографии, вложения, приглашения, роли,
  совместное редактирование, refresh token, уведомления, календарь и
  production-оплаты. Они относятся к следующим этапам.
- [ ] Не создавать отдельные таблицы для `overview` и `budget`: это
  вычисляемые представления по проектам, комнатам и покупкам.
- [ ] До написания покупки согласовать набор машинных категорий и добавить его
  в `API.MD`. Например: `materials`, `plumbing`, `electrical`, `furniture`,
  `appliances`, `decor`, `services` и `other`. Интерфейс отображает
  локализованные подписи, API хранит машинные значения.

Проверка: можно без кода словами пройти путь
`регистрация → вход → проект → комната → покупка → бюджет` и для каждого шага
назвать API-маршрут и данные.

## 2. Подготовить структуру backend и общие правила API

### 2.1. Разделить слои приложения

- [ ] Создать пакеты с понятными ролями:

  ```text
  backend/app/
  ├── api/          # routers, зависимости, обработчики ошибок
  ├── core/         # конфигурация и безопасность
  ├── db/           # Base, session и тестовые помощники
  ├── models/       # SQLAlchemy-модели
  ├── schemas/      # Pydantic-схемы входа и ответа
  └── services/     # сценарии, проверки владения и расчёты
  ```

- [ ] Не помещать SQL-запросы, проверку JWT и сериализацию ответа в один
  router-файл. Router принимает HTTP-запрос и вызывает сервис; сервис работает
  с сессией и моделями; Pydantic-схема определяет внешний JSON.
- [ ] Подключить routers через один `APIRouter(prefix="/api/v1")` в
  `backend/app/main.py`. Технический `/api/v1/health` остаётся совместимым с
  существующим тестом.
- [ ] Создать dependency `get_db_session`, которая открывает SQLAlchemy-сессию
  на запрос, передаёт её endpoint'у и гарантированно закрывает после ответа.
  Коммит не должен выполняться «магически» в dependency: сервис явно решает,
  когда сохранить успешное изменение.
- [ ] Добавить единый обработчик ошибок приложения в формате
  `application/problem+json` из `API.MD`: валидация — `422`, конфликт email —
  `409`, отсутствие/ошибка токена — `401` с `WWW-Authenticate: Bearer`,
  недоступный или чужой ресурс — `404`.

### 2.2. Подготовить настройки безопасности

- [ ] Добавить необходимые зависимости для безопасного хеширования паролей и
  подписи/проверки JWT. Выбрать одну библиотеку для хеширования и одну для JWT;
  не реализовывать криптографию вручную.
- [ ] Расширить `Settings` параметрами `JWT_SECRET_KEY`, `JWT_ALGORITHM`,
  `JWT_ISSUER`, `JWT_AUDIENCE` и `ACCESS_TOKEN_EXPIRE_SECONDS`. Для MVP срок
  access token — `900` секунд.
- [ ] Добавить безопасные примерные значения и комментарии в `.env.example`,
  но не добавлять настоящий `JWT_SECRET_KEY` в Git. В локальном `.env` секрет
  должен быть длинным случайным значением.
- [ ] Создать `backend/app/core/security.py` с отдельными функциями:
  `hash_password`, `verify_password`, `create_access_token` и
  `decode_access_token`.
- [ ] В JWT указывать только необходимые claims: `sub` (UUID аккаунта),
  `iat`, `exp`, `iss`, `aud` и `jti`. При чтении токена проверять алгоритм,
  подпись, срок, issuer и audience.
- [ ] Создать dependency `get_current_account`: она читает Bearer token,
  проверяет JWT, получает аккаунт по `sub` и возвращает `401`, если токен
  неверный, просрочен или аккаунт больше не существует.

Проверка: секрет не выводится в ошибке, `password_hash` не является полем
ни одной response-схемы, а неверный Bearer token даёт problem+json с `401`.

## 3. Подготовить тестовые данные и изоляцию интеграционных тестов

- [ ] Создать набор test factories или небольших helpers для аккаунта,
  авторизации, проекта, комнаты и покупки. Они должны создавать уникальные
  email и UUID, чтобы тесты не зависели от порядка запуска.
- [ ] Для интеграционных тестов, которые создают записи, после каждого теста
  очищать тестовые таблицы в порядке зависимостей либо пересоздавать схему.
  Очистка работает только через `TEST_DATABASE_URL` с именем БД, оканчивающимся
  на `_test`.
- [ ] Не очищать рабочую БД и не использовать `docker compose down -v` для
  обычного запуска проекта. Если нужен полный сброс, удаляется только test
  container и его volume.
- [ ] Добавить тесты двух пользователей: данные, созданные первым, не видны
  второму; запрос по чужому UUID возвращает `404`.
- [ ] Пометить тесты, требующие настоящую PostgreSQL и миграции, маркером
  `integration`. Быстрые проверки схем Pydantic, JWT и HTTP-обработчиков без
  БД оставлять unit-тестами.

Проверка: `uv run pytest -m integration` проходит два раза подряд без
зависимости от данных предыдущего запуска.

## 4. Модели и миграции предметной области

Перед каждой миграцией выполните `uv run alembic current`. После изменения
моделей создайте новую ревизию, вручную прочитайте `upgrade()` и `downgrade()`,
примените `uv run alembic upgrade head` и выполните `uv run alembic check`.
В `alembic/env.py` импортируйте каждую новую модель, иначе Alembic не увидит
её metadata.

### 4.1. Проекты

- [ ] Создать модель `Project` и отдельную миграцию `create projects table`.
- [ ] Добавить поля:

  | Поле | Правило |
  | --- | --- |
  | `id` | UUID, первичный ключ, генерируется сервером. |
  | `owner_id` | UUID, обязательный внешний ключ на `accounts.id`, индекс. |
  | `name` | Обязательное название после trim, от 1 до 120 символов. |
  | `property_type` | Одно из `apartment`, `house`, `other`. |
  | `address` | Необязательный адрес до 500 символов. |
  | `planned_budget` | `NUMERIC(14, 2)`, неотрицательное число. |
  | `is_archived` | `BOOLEAN`, по умолчанию `false`. |
  | `archived_at` | Время с часовым поясом или `NULL`. |
  | `created_at` / `updated_at` | Серверные даты-время с часовым поясом. |

- [ ] Добавить ограничение `planned_budget >= 0` и индексы для типичных
  запросов владельца: как минимум `owner_id` и `owner_id + is_archived`.
- [ ] Настроить связь `Account.projects` и `Project.owner`. Каскады не должны
  скрыто удалять архивный проект.

### 4.2. Комнаты

- [ ] Создать модель `Room` и миграцию `create rooms table`.
- [ ] Добавить `id`, `project_id` (обязательный FK на `projects.id`),
  `name` от 1 до 120 символов, `created_at` и `updated_at`.
- [ ] Добавить индекс `project_id` и связь `Project.rooms`.
- [ ] Выбрать явное поведение при удалении комнаты: для MVP удаление комнаты,
  у которой есть покупки, должно вернуть `409 Conflict` с понятным сообщением,
  а не потерять покупки каскадом.

### 4.3. Покупки

- [ ] Создать модель `Purchase` и миграцию `create purchases table`.
- [ ] Добавить `id`, `project_id` (FK), `room_id` (необязательный FK),
  `name`, `category`, `planned_amount`, `actual_amount`, `status`, `note`,
  `purchased_on`, `created_at` и `updated_at`.
- [ ] Использовать `NUMERIC(14, 2)` для обеих сумм; `planned_amount` не может
  быть отрицательной. `actual_amount` либо `NULL`, либо неотрицательна.
- [ ] Хранить статус как ограниченный набор `planned`, `purchased`,
  `cancelled` (например, через PostgreSQL CHECK constraint или SQLAlchemy
  Enum). Не хранить русские подписи в БД.
- [ ] Зафиксировать ограничение: `actual_amount` и `purchased_on` разрешены
  только для `purchased`; для `planned` и `cancelled` они `NULL`. Проверять
  это и в Pydantic, и в сервисе; DB constraint добавить, если он остаётся
  читаемым и переносимым.
- [ ] Добавить индексы `project_id`, `room_id`, `project_id + status` и
  `project_id + purchased_on` для списков и агрегатов.

Проверка раздела: чистая тестовая БД после `uv run alembic upgrade head`
содержит `accounts`, `projects`, `rooms`, `purchases` и `alembic_version`.

## 5. Реализовать регистрацию, вход и профиль

### 5.1. Схемы и сервис аккаунта

- [ ] Создать отдельные Pydantic-схемы `AccountRegister`, `AccountPublic`,
  `LoginRequest`, `AccessTokenResponse` и `ProfileUpdate`. У response-схемы
  всегда задать `from_attributes=True`.
- [ ] Валидировать имя после trim: от 1 до 100 символов. Email нормализовать
  в единый вид до проверки уникальности (например, trim и lowercase).
- [ ] Определить минимальное правило пароля для MVP (например, минимум
  12 символов) и вернуть структурированную `422`-ошибку, если правило не
  выполнено. Не добавлять чрезмерно сложные правила без продуктовой причины.
- [ ] Создать сервис регистрации: проверить уникальность email, захешировать
  пароль, сохранить аккаунт и вернуть публичный объект без хеша.
- [ ] Преобразовать нарушение уникальности email в `409 Conflict`. Не
  полагаться только на предварительный `SELECT`: БД остаётся последней защитой
  от гонки запросов.

### 5.2. Маршруты и тесты

- [ ] Реализовать `POST /api/v1/auth/register`. Успех — `201 Created` и
  `AccountPublic`; регистрация не выдаёт токен.
- [ ] Реализовать `POST /api/v1/auth/login`. Неверный email и неверный пароль
  должны давать одинаковый `401 Unauthorized` без раскрытия причины. Успех —
  `AccessTokenResponse`.
- [ ] Реализовать защищённый `GET /api/v1/profile`.
- [ ] Реализовать защищённый `PATCH /api/v1/profile` только для `name`.
  Email и пароль не изменяются в этом сценарии.
- [ ] Написать unit-тесты хеширования, проверки срока/issuer/audience JWT и
  преобразования ошибок.
- [ ] Написать интеграционные HTTP-тесты: успешная регистрация, конфликт
  email, успешный/неуспешный вход, профиль без токена, с неверным токеном и с
  корректным токеном, изменение имени.

Проверка: `password_hash` никогда не появляется в JSON или OpenAPI response;
`GET /api/v1/profile` для токена другого пользователя возвращает данные только
этого пользователя.

## 6. Реализовать проекты

- [ ] Создать схемы `ProjectCreate`, `ProjectUpdate`, `ProjectPublic` и
  `ProjectListResponse`. В create нельзя передать `owner_id`, `id`,
  `archived_at` или timestamps.
- [ ] Реализовать сервис `get_owned_project_or_404(project_id, account, db)`.
  Он всегда фильтрует запрос по `project_id` и `owner_id`; это единая точка
  проверки владения.
- [ ] Реализовать `POST /api/v1/projects` и создать проект для текущего
  аккаунта.
- [ ] Реализовать `GET /api/v1/projects` с `limit`, `offset`, `archived`,
  `property_type`, `search` и `sort` по контракту. По умолчанию показывать
  только неархивные проекты.
- [ ] Реализовать `GET /api/v1/projects/{project_id}` и `PATCH`. Разрешить
  изменить название, тип, адрес, бюджет и `is_archived`; `archived_at` ставит
  и очищает сервер.
- [ ] Не добавлять `DELETE /projects/{project_id}` в MVP.
- [ ] Добавить тесты пагинации, поиска, сортировки, архивации/возврата из
  архива, валидации отрицательного бюджета и недоступности проекта другому
  аккаунту.

Проверка: пользователь с двумя проектами видит только свои проекты, а
архивный проект исчезает из списка по умолчанию и появляется при
`?archived=true`.

## 7. Реализовать комнаты

- [ ] Создать схемы `RoomCreate`, `RoomUpdate`, `RoomPublic` и
  `RoomListResponse`.
- [ ] Реализовать `POST /api/v1/projects/{project_id}/rooms` и
  `GET /api/v1/projects/{project_id}/rooms`. Доступ к проекту проверяется до
  создания или выдачи комнат.
- [ ] Реализовать `GET/PATCH/DELETE /api/v1/rooms/{room_id}`. Все операции
  проверяют владельца через проект комнаты.
- [ ] Для `DELETE` с существующими покупками вернуть `409` с problem+json и
  объяснить в `detail`, что сначала нужно перенести или удалить покупки.
- [ ] Добавить тесты чужого проекта/комнаты, сортировки комнат, валидации
  имени и запрета удаления комнаты с покупками.

Проверка: комнату нельзя присоединить к чужому проекту, а удаление пустой
комнаты отвечает `204 No Content`.

## 8. Реализовать покупки

### 8.1. Правила данных

- [ ] Создать схемы `PurchaseCreate`, `PurchaseUpdate`, `PurchasePublic` и
  `PurchaseListResponse`. Все суммы в JSON остаются строками, например
  `"12500.50"`.
- [ ] Валидировать `room_id`: если он указан, комната существует, доступна
  текущему пользователю и относится к тому же `project_id`, что покупка.
- [ ] Валидировать статус:
  - `planned`: `actual_amount` и `purchased_on` равны `null`;
  - `purchased`: `actual_amount` и `purchased_on` обязательны;
  - `cancelled`: `actual_amount` и `purchased_on` равны `null`.
- [ ] При PATCH пересчитывать эти правила из итогового состояния объекта, а
  не только из переданных полей. Например, нельзя изменить статус на
  `purchased` и забыть фактическую сумму.
- [ ] Не интерпретировать пустую строку как деньги, UUID или дату. Клиент
  передаёт `null` для необязательного поля; backend отвечает `422` на
  некорректный тип.

### 8.2. Маршруты и списки

- [ ] Реализовать `POST /api/v1/projects/{project_id}/purchases` и
  `GET /api/v1/projects/{project_id}/purchases`.
- [ ] Реализовать `GET/PATCH/DELETE /api/v1/purchases/{purchase_id}`.
- [ ] Реализовать фильтры `status`, `room_id`, `category`,
  `purchased_from` и `purchased_to`, включая повторяемые `status`,
  `room_id` и `category` по контракту.
- [ ] Реализовать разрешённые сортировки и стабильный порядок с дополнительным
  `id`. Некорректный `sort` или диапазон дат возвращают `422`.
- [ ] После удаления покупки возвращать `204`. Удаление не должно менять
  проект, комнату или другие покупки.
- [ ] Добавить интеграционные тесты создания, изменения статуса, фильтров,
  сортировки, диапазона дат, чужой покупки и проверки принадлежности комнаты.

Проверка: купленная покупка влияет на фактические расходы, отменённая не
участвует в плановой сумме, а данные другого пользователя недоступны.

## 9. Реализовать «Обзор» и «Бюджет»

- [ ] Создать read-only схемы `ProjectOverview` и `ProjectBudget` строго по
  полям из `API.MD`.
- [ ] Реализовать `GET /api/v1/projects/{project_id}/overview`. Он возвращает
  все комнаты с количеством покупок, общее число комнат и покупок, счётчики
  статусов, бюджет, фактические траты, остаток и до пяти последних покупок.
- [ ] Реализовать `GET /api/v1/projects/{project_id}/budget`. Он возвращает
  `planned_budget`, `planned_purchases_total`, `actual_spent`,
  `remaining_budget` и суммы по категориям.
- [ ] Считать `actual_spent` только по `purchased` и `actual_amount`.
  `planned_purchases_total` — сумма плановых значений покупок, кроме
  `cancelled`. `remaining_budget = planned_budget - actual_spent` и может
  быть отрицательным.
- [ ] Считать агрегаты на стороне PostgreSQL (`SUM`, `COUNT`, `GROUP BY`), а
  не загружать все покупки в Python для каждого запроса.
- [ ] Убедиться, что пустой проект отдаёт нули и пустые массивы, а не `null`
  в числовых итогах.
- [ ] Добавить тесты смешанного набора статусов, нескольких категорий,
  отсутствия покупок, превышения бюджета и чужого проекта.

Проверка: ручной расчёт по нескольким покупкам совпадает с ответами
`overview` и `budget` до копейки.

## 10. Подготовить frontend к настоящему приложению

- [ ] Добавить клиентскую маршрутизацию и структуру экранов. Минимальные
  публичные маршруты: `/login` и `/register`; защищённые: `/projects`,
  `/projects/:projectId/overview`, `rooms`, `purchases`, `budget` и
  `settings`.
- [ ] Создать единый API-клиент поверх `fetch`: базовый URL из
  `VITE_API_URL`, JSON-заголовки, Bearer token, обработка `204` и
  преобразование problem+json в типизированную ошибку.
- [ ] Описать TypeScript-типы по `API.MD`. Не дублировать «похожие» типы в
  каждом компоненте.
- [ ] Хранить access token для MVP в `sessionStorage`, а текущий профиль — в
  React state. Не писать токен в URL, query-параметры, логи или
  `VITE_*`-переменные. При `401` очищать сессию и переводить пользователя на
  `/login`.
- [ ] Создать `RequireAuth`, который не показывает защищённый экран без
  токена. Помнить: это UX, а не замена backend-проверки.
- [ ] Создать общий каркас авторизованной части: шапка, название выбранного
  проекта, навигация из `DESIGN.MD`, область контента, состояния загрузки,
  пустого результата и ошибки.
- [ ] Использовать существующие макеты из `design/images/01-web` как
  источник компоновки. Не смешивать Warm Interior и Graphite Hi-Tech на
  разных экранах: выбрать одну тему реализации и сохранять её последовательно.

Проверка: при обновлении страницы с действующим токеном frontend запрашивает
`GET /profile` и восстанавливает сессию; при `401` возвращает на страницу
входа.

## 11. Реализовать экраны по пользовательскому пути

### 11.1. Регистрация, вход и профиль

- [ ] Создать формы регистрации и входа с доступными `label`, подсказками,
  сообщениями серверной валидации и отключением повторной отправки на время
  запроса.
- [ ] После успешной регистрации вести пользователя на вход; не считать
  регистрацию скрытым автоматическим входом, потому что API не выдаёт токен.
- [ ] После входа сохранить токен, запросить профиль и перейти на список
  проектов.
- [ ] Добавить управление отображаемым именем в профиль/меню пользователя
  через `PATCH /profile`.
- [ ] Добавить выход: очистить `sessionStorage` и React state, затем перейти
  на `/login`.

### 11.2. «Мои проекты» и настройки проекта

- [ ] Реализовать список проектов с начальным состоянием без проектов,
  созданием первого проекта, поиском, архивными проектами и пагинацией.
- [ ] Создать форму проекта: название, тип объекта, адрес и плановый бюджет.
  Денежное поле отправляет строку, подготовленную к формату API, а не число
  `JavaScript Number`.
- [ ] Сделать экран настроек проекта: редактирование полей, архивирование и
  возврат из архива. Удаления проекта в UI не предлагать.
- [ ] После создания выбрать новый проект и направить пользователя на
  `overview`.

### 11.3. Обзор и комнаты

- [ ] Создать экран обзора: показатели бюджета, комнаты, счётчики статусов и
  последние покупки по ответу `overview`.
- [ ] Реализовать список комнат, создание, переименование и удаление.
- [ ] При `409` удаления комнаты показать понятное сообщение и путь к
  связанным покупкам, а не общий текст «что-то пошло не так».
- [ ] На экране комнаты показывать связанные покупки, используя фильтр
  `room_id`, а не отдельный дублирующий API.

### 11.4. Покупки и бюджет

- [ ] Реализовать список покупок с фильтрами статуса, комнаты, категории и
  диапазона даты, а также сортировкой и пагинацией.
- [ ] Создать форму покупки. Поля фактической суммы и даты покупки появляются
  и становятся обязательными только при `purchased`; при смене на
  `planned`/`cancelled` frontend очищает их перед отправкой.
- [ ] Реализовать просмотр, изменение и удаление покупки. Перед удалением
  показать подтверждение с названием покупки.
- [ ] Создать экран бюджета из `GET .../budget`: общий бюджет, плановые
  покупки, фактические траты, остаток и разбивка по категориям.
- [ ] Визуально выделять отрицательный остаток, но не запрещать его: API
  допускает превышение бюджета.

Проверка раздела: на узком экране основные действия доступны без
горизонтальной прокрутки, а пользователь понимает состояние загрузки, пустого
списка и ошибки запроса.

## 12. Проверить доступность, адаптивность и ошибки

- [ ] Использовать семантические заголовки, `label` для каждого поля,
  видимый `:focus-visible` и доступные названия кнопок и иконок.
- [ ] Не передавать цветом единственный смысл статуса покупки или превышения
  бюджета: добавлять текст и/или иконку.
- [ ] Проверить навигацию только клавиатурой в формах, диалогах подтверждения
  и меню проекта.
- [ ] Проверить desktop и мобильную ширину для экранов auth, projects,
  overview, rooms, purchases, budget и settings.
- [ ] Единообразно обрабатывать `401`, `404`, `409`, `422` и сетевую ошибку:
  показывать пользователю понятное действие, а технические детали не
  подставлять в DOM без экранирования.

## 13. Обновить документацию и автоматические проверки

- [ ] При уточнении категорий, ограничений полей или поведения удаления
  обновить [API.MD](API.MD) в том же изменении, что backend и frontend.
- [ ] Обновить [RUNBOOK.MD](RUNBOOK.MD): раздел CI уже существует, поэтому
  убрать устаревшие формулировки о будущем workflow; добавить команды
  сценария 1.3, если они нужны для локальной разработки.
- [ ] Обновить `README.MD` и `frontend/README.md`: временная страница
  health-check больше не описывает фактический пользовательский интерфейс.
- [ ] Добавить backend unit-тесты для валидации, безопасности и расчётов;
  добавить integration-тесты HTTP-ресурсов и владения.
- [ ] Добавить frontend component/API-тесты для критичных сценариев: вход,
  создание проекта, валидация покупки, `401` и `409`. Выбрать тестовый
  инструментарий до написания тестов и зафиксировать команды в документации.
- [ ] Оставить CI как единый источник проверки: новые тесты должны
  укладываться в существующие backend/frontend jobs без секретов и локального
  `.env`.

После каждого законченного среза выполняйте:

```bash
uv run ruff check .
uv run ruff format --check .
uv run pytest -m "not integration"
uv run pytest -m integration
uv run alembic check

cd frontend
npm run lint
npm run build
```

## 14. Критерии завершения 1.3

- [ ] Новый пользователь может зарегистрироваться, войти и изменить своё имя.
- [ ] Любой защищённый endpoint требует корректный JWT, а данные другого
  аккаунта отвечают `404`.
- [ ] Пользователь создаёт, изменяет, архивирует и возвращает собственный
  проект; физического удаления проектов нет.
- [ ] Пользователь создаёт, изменяет и удаляет комнаты, а комната с покупками
  не удаляется молча.
- [ ] Пользователь создаёт, меняет, фильтрует и удаляет покупки; статусы и
  суммы валидируются на backend.
- [ ] Overview и budget рассчитывают показатели в соответствии с `API.MD`.
- [ ] Frontend реализует экраны `Мои проекты`, `Обзор`, `Комнаты`,
  `Покупки`, `Бюджет` и `Настройки проекта` в едином визуальном направлении.
- [ ] Локальные и CI-проверки backend/frontend проходят; интеграционные
  тесты не оставляют данных в тестовой БД.
- [ ] Документация описывает реализованное поведение, а не временный
  health-check интерфейс.

## Рекомендуемый порядок коммитов

Держите изменения маленькими и проверяемыми:

1. общая структура API, настройки безопасности и обработчики ошибок;
2. `Project`: модель → миграция → backend API → тесты;
3. `Room`: модель → миграция → backend API → тесты;
4. `Purchase`: модель → миграция → backend API → тесты;
5. auth/profile и проверка владения;
6. overview/budget и тесты расчётов;
7. frontend foundation, auth и список проектов;
8. frontend screens комнат, покупок, обзора, бюджета и настроек;
9. доступность, документация, полный регрессионный прогон.

Не объединяйте в один коммит несвязанные миграции, редизайн и изменение
контракта. Понятная история упрощает review, откат и поиск причины ошибки.
