# 1.2. Собрать основу — подробный план

Этот документ детализирует пункт 1.2 из [основного плана](PLAN.MD). Он рассчитан на обучение: выполняем одну небольшую задачу, проверяем результат и только затем переходим дальше.

## Результат этапа

В репозитории работают API, web-клиент и PostgreSQL. Проект поднимается одной командой Docker Compose, изменения схемы БД проходят через миграции, health-check и тесты выполняются локально и в CI.

## Принципы работы

- Не пишем функциональность проектов и покупок до готовности основы.
- Каждый шаг должен иметь понятный результат и команду проверки.
- Секреты не храним в Git: используем `.env` и `.env.example`.
- Начинаем с минимальной реализации; усложняем, когда появляется практическая потребность.
- Все команды `uv` выполняем из корня репозитория, где находятся `pyproject.toml` и `uv.lock`.

## 1. Каркас и локальный запуск API

- [x] Создать папки `backend/app`, `backend/tests`, `frontend` и `plan`.
- [x] Инициализировать Python-проект через корневой `pyproject.toml`.
- [x] Добавить FastAPI, Uvicorn, SQLAlchemy, Alembic, Psycopg, Pydantic Settings, Pytest и HTTPX.
- [x] Создать `backend/app/main.py` с приложением FastAPI.
- [x] Реализовать `GET /api/v1/health`, временно без проверки БД, с ответом `{"status": "ok"}`.

Проверка: из корня репозитория выполнить `uv run uvicorn backend.app.main:app --reload`, затем открыть `http://127.0.0.1:8000/docs`.

## 2. PostgreSQL и конфигурация

- [x] Создать корневой файл Docker Compose (`docker-compose.yml` или `docker-compose.yaml`) с сервисом PostgreSQL.
- [x] Создать `.env.example` с именем БД, пользователем, паролем и портами без реальных секретов.
- [x] Добавить `backend/app/core/config.py`: настройки читаются из переменных окружения.
- [x] Настроить подключение к БД для локального запуска и Docker: локально `DB_HOST=localhost`, а внутри сервиса `api` Compose — `DB_HOST=apartment_db` (имя сервиса PostgreSQL). Допустимо перейти на единую переменную `DATABASE_URL`, если она формируется без дублирования секрета.
- [x] Убедиться, что API получает настройки, но пока не обязан обращаться к БД на каждом запросе.

Проверка: `docker compose up -d <имя-сервиса-БД>` и подключение через `docker compose exec <имя-сервиса-БД> psql -U <пользователь> -d <база>`.

## 3. SQLAlchemy и миграции

В этом разделе появятся три связанных слоя:

```text
модель Account в Python → SQLAlchemy metadata → миграция Alembic → таблица accounts в PostgreSQL
```

SQLAlchemy описывает таблицы классами Python и создаёт подключения к БД. Alembic хранит историю изменений схемы: вместо ручного создания таблиц каждый разработчик применяет одинаковые версии миграций.

### 3.1. Подключение SQLAlchemy

- [x] Создать пакет `backend/app/db` и файл `base.py`.
- [x] В `base.py` создать класс `Base`, наследующийся от `DeclarativeBase`. Все модели будут наследоваться от него.
- [x] Создать модуль подключения, например `backend/app/db/session.py`.
- [x] В нём создать синхронный `Engine` через ленивую функцию `get_engine()`, вызывающую `create_engine(get_settings().database_url)`: URL формируется в `core/config.py`, пароль и хост не дублируются.
- [x] Создать фабрику сессий `get_session()` через `sessionmaker`. Сессия будет единицей работы с БД внутри будущих API-запросов.
- [x] Не создавать соединение при импорте модуля: `create_engine()` только настраивает Engine; настоящее соединение откроется при первом запросе к БД.

На этом этапе используем синхронный SQLAlchemy: у нас драйвер `psycopg`, а первые endpoint'ы пока обычные `def`. Асинхронный вариант намеренно не добавляем, чтобы не смешивать два подхода.

Проверка: маленький временный Python-скрипт или однострочная команда импортирует Engine и выполняет `SELECT 1`. Результат должен быть `1`.

### 3.2. Подготовка Alembic

- [x] Из корня репозитория выполнить `uv run alembic init alembic`. Появятся папка `alembic/`, файл `alembic.ini` и папка `alembic/versions/` для будущих миграций.
- [x] Оставить в `alembic.ini` технические настройки Alembic, но не хранить там адрес БД и пароль.
- [x] В `alembic/env.py` получить URL через `get_settings()` из `backend.app.core.config` и передать его в конфигурацию Alembic программно; при интеграционном тесте использовать отдельно проверенный `TEST_DATABASE_URL`.
- [x] В `alembic/env.py` импортировать `Base` и присвоить `target_metadata = Base.metadata`. После создания `Account` в разделе 3.3 отдельно импортировать модель в `env.py`, иначе Alembic не узнает о таблице и автогенерация будет пустой.

Проверка: команда `uv run alembic current` запускается без ошибки и использует БД из `.env`.

### 3.3. Первая модель

- [x] Создать пакет `backend/app/models` и файл `account.py`.
- [x] Создать класс `Account(Base)` и задать имя таблицы `accounts` через `__tablename__`.
- [x] Добавить поле `id`: UUID, первичный ключ, значение создаётся автоматически.
- [x] Добавить поле `email`: строка, обязательное значение, уникальный индекс. В базе не должно быть двух аккаунтов с одинаковым email.
- [x] Добавить поле `password_hash`: обязательная строка. Храним только хеш пароля, никогда не исходный пароль.
- [x] Добавить поле `created_at`: дата и время создания записи с часовым поясом; значение по умолчанию задаёт сама БД.
- [x] Проверить, что `alembic/env.py` импортирует `Account`, иначе модель не попадёт в `Base.metadata`.

На этом шаге не добавляем регистрацию, JWT или проверку пароля — только описание будущей таблицы.

### 3.4. Первая миграция

- [x] Создать миграцию командой `uv run alembic revision --autogenerate -m "create accounts table"`.
- [x] Открыть созданный файл в `alembic/versions/` и прочитать функции `upgrade()` и `downgrade()`. Автогенерацию всегда проверяем до применения: это подсказка Alembic, а не безусловно правильный SQL.
- [x] Применить миграцию командой `uv run alembic upgrade head`.
- [x] Убедиться, что появилась таблица `accounts` и служебная таблица `alembic_version`.
- [x] Проверить историю командой `uv run alembic current` или `uv run alembic history`.

Проверка в PostgreSQL: `docker compose exec <имя-сервиса-БД> psql -U <пользователь> -d <база>`, затем команда `\\dt` покажет список таблиц.

## Критерии завершения раздела 3

- [x] `settings.database_url` используется и приложением, и Alembic; URL не копируется в несколько файлов.
- [x] В Git добавлены `alembic/`, `alembic.ini` и первая миграция, но не добавляется `.env`.
- [x] Чистая БД после `uv run alembic upgrade head` содержит таблицы `accounts` и `alembic_version`.

## 4. Контракт API и доступ

Этот раздел пока не добавляет endpoint'ы, модели или JWT-код. Сначала создаём документ `plan/API.MD` — общий договор между backend, web-клиентом и будущими мобильными приложениями. Он позволит реализовывать сценарий 1.3 без неявных решений о маршрутах, формате данных и правах.

### 4.1. Общие правила API

- [x] Создать `plan/API.MD` с заголовком «Контракт API Web-MVP» и кратко указать, что документ фиксирует правила до реализации endpoint'ов.
- [x] Зафиксировать общий префикс для прикладных маршрутов: `/api/v1`. Например, список проектов будет доступен по `GET /api/v1/projects`.
- [x] Указать, что API принимает и возвращает JSON в UTF-8. Для запросов с телом клиент отправляет `Content-Type: application/json`.
- [x] Выбрать стиль именования JSON-полей: `snake_case`, например `created_at` и `password_hash`.
- [x] Зафиксировать формат идентификаторов: UUID передаётся строкой.
- [x] Зафиксировать формат денег: десятичная строка, например `"12500.50"`, а не `float`, чтобы избежать ошибок округления.
- [x] Зафиксировать формат даты и времени: ISO 8601 с часовым поясом, например `2026-08-11T18:30:00+03:00`; дата без времени — `YYYY-MM-DD`.
- [x] Указать успешные статусы: `200 OK` для чтения и обновления, `201 Created` для создания и `204 No Content` для удаления.

### 4.2. Ошибки и пагинация

- [x] Описать единый формат ошибки `application/problem+json` с полями `type`, `title`, `status`, `detail` и необязательным `errors` для ошибок конкретных полей.
- [x] Зафиксировать назначение основных HTTP-статусов: `400` — некорректный запрос, `401` — нет или неверен токен, `403` — действие запрещено, `404` — ресурс не найден, `409` — конфликт данных, `422` — ошибка валидации.
- [x] Зафиксировать пагинацию для списочных маршрутов через query-параметры `limit` и `offset`. Пример: `GET /api/v1/projects?limit=20&offset=0`.
- [x] Описать ответ списка: объект с `items`, `limit`, `offset` и `total`, чтобы клиент мог построить постраничный интерфейс.

### 4.3. Ресурсы MVP

- [x] Создать в `plan/API.MD` таблицу ресурсов: `auth`, `projects`, `rooms`, `purchases`, а также read-only представлений `overview` и `budget`; для каждого указать назначение и основные будущие маршруты.
- [x] Для `auth` и `profile` зафиксировать будущие операции регистрации, входа и работы с текущим пользователем: `POST /auth/register`, `POST /auth/login`, `GET/PATCH /profile`.
- [x] Для `projects` зафиксировать список, создание, просмотр, изменение и архивирование проекта без физического удаления в MVP.
- [x] Для `rooms` зафиксировать создание и список комнат внутри проекта, а также изменение и удаление комнаты.
- [x] Для `purchases` зафиксировать создание и список покупок внутри проекта, а также просмотр, изменение и удаление покупки.
- [x] Указать, что `GET /api/v1/health` — технический маршрут проверки API и не является пользовательским ресурсом.

### 4.4. Авторизация и доступ

- [x] Зафиксировать публичные операции: регистрация, вход и технический health-check не требуют access token.
- [x] Зафиксировать, что `GET/PATCH /profile`, все операции с проектами, комнатами и покупками, а также представления `overview` и `budget` требуют входа.
- [x] Выбрать JWT access token: после входа API возвращает токен, а клиент отправляет его в заголовке `Authorization: Bearer <access_token>`.
- [x] Указать, что токен не передаётся в URL или теле обычного запроса, а `password_hash` никогда не возвращается клиенту.
- [x] Не добавлять refresh token в MVP. Вернуться к решению при реализации входа, когда будут определены срок жизни сессии и безопасное хранение токенов в клиентах.
- [x] Описать правило владения: пользователь читает и меняет только собственные проекты; комнаты и покупки доступны только через принадлежащий ему проект.
- [x] Указать, что проверку владения выполняет backend в каждом защищённом запросе. Интерфейс клиента не является проверкой безопасности.
- [x] Выбрать поведение для чужих ресурсов: не раскрывать данные и возвращать `404 Not Found`.
- [x] Зафиксировать границу MVP: приглашения, совместный доступ и роли будут добавлены после MVP отдельным изменением контракта и модели прав.

Проверка: прочитать `plan/API.MD` как разработчик клиента. Для каждого ресурса должно быть понятно, какой маршрут вызвать, нужен ли токен, как выглядят дата, деньги, успешный ответ и ошибка. После этого можно отметить пункты раздела как выполненные.

Результат: документ согласован до написания endpoint'ов сценария 1.3.

### 4.5. Подготовить аккаунт к регистрации

- [x] В модели `Account` добавить обязательное поле отображаемого имени `name` длиной от 1 до 100 символов.
- [x] Создать новую миграцию Alembic для колонки `accounts.name`, проверить `upgrade()` и `downgrade()`, затем применить её командой `uv run alembic upgrade head`.
- [x] Проверить в PostgreSQL, что `accounts.name` существует, обязательно для заполнения и соответствует модели.

Первая миграция `create accounts table` остаётся неизменяемой: любое новое изменение схемы оформляется отдельной миграцией.

Проверка: `uv run alembic current` показывает новую ревизию как `head`; `uv run alembic check` не обнаруживает незафиксированных изменений модели.

Шпаргалка по командам и правилам миграций: [Alembic](ALEMBIC.MD).

## 4.6. Стиль Python-кода

- [x] Добавить Ruff как dev-зависимость и зафиксировать минимальные правила: ошибки Python, стиль и порядок импортов.
- [x] Настроить Ruff в `pyproject.toml`: Python 3.12+, длина строки 100 символов, исключение сгенерированных миграций Alembic.
- [x] Запустить `uv run ruff check .` и `uv run ruff format --check .`; исправить замечания до успешного завершения обеих команд.
- [x] Добавить Git pre-commit hook через `pre-commit`: перед `git commit` запускать `uv run ruff check .` и `uv run ruff format --check .`. При ошибках hook должен завершаться с ненулевым кодом, показывать замечания Ruff и отменять создание коммита; после исправления повторный `git commit` проходит без ручного запуска линтера.
  - Добавить инструмент в dev-зависимости из корня репозитория: `uv add --dev pre-commit`. Команда изменит `pyproject.toml` и `uv.lock`; оба файла должны попасть в коммит, чтобы у всех использовалась одна версия инструмента.
  - Создать в корне `.pre-commit-config.yaml`. В нём объявить два локальных hook (`repo: local`), потому что Ruff уже установлен через `uv`:

    ```yaml
    repos:
      - repo: local
        hooks:
          - id: ruff-check
            name: Ruff lint
            entry: uv run ruff check .
            language: system
            pass_filenames: false
          - id: ruff-format
            name: Ruff format
            entry: uv run ruff format --check .
            language: system
            pass_filenames: false
    ```

    `language: system` запускает команду в текущем окружении проекта, а не создаёт отдельное виртуальное окружение pre-commit. `pass_filenames: false` важен: команды сами получают корень проекта (`.`), поэтому hook проверяет все файлы по тем же правилам, что и ручной запуск.
  - Установить hook в локальный Git-клон командой `uv run pre-commit install`. Она создаст/обновит `.git/hooks/pre-commit`; этот файл не коммитится, поэтому каждый разработчик после `uv sync` должен один раз выполнить ту же команду.
  - Сразу проверить конфигурацию без создания коммита: `uv run pre-commit run --all-files`. Оба hook должны завершиться со статусом `Passed`.
  - Проверить блокировку: временно внести в Python-файл ошибку Ruff, выполнить `git add <файл>` и `git commit -m "проверка hook"`. Коммит должен быть отменён. Исправить ошибку, добавить файл снова и повторить коммит — теперь он должен пройти. Не создавать намеренно плохой коммит в общей ветке.
  - При обычном `git commit` hook проверяет текущее рабочее дерево. Проверка может занять немного дольше, чем проверка только добавленных файлов, зато поведение совпадает с `uv run ruff check .` и не пропускает ошибку в неиндексированном связанном файле.
  - Если hook сообщает ошибку, сначала выполнить показанную команду Ruff, исправить код либо отформатировать его через `uv run ruff format .`, затем повторить `git add` и `git commit`.
  - Не использовать `git commit --no-verify` в обычной работе: этот флаг намеренно пропускает hook и нужен только для исключительных случаев. Hook является локальной защитой; CI всё равно должен отдельно запускать Ruff, чтобы проверка не обходилась этим флагом.

Шпаргалка по командам и правилам: [Ruff](RUFF.MD).

## 5. Тесты и настоящий health-check

На этом шаге временный health-check `{"status": "ok"}` становится настоящей проверкой готовности приложения: API отвечает, только если может выполнить простой запрос к PostgreSQL. Тесты не должны зависеть от реальной локальной БД и должны проверять оба исхода — доступность и недоступность БД.

### 5.1. Подготовка Pytest и тестового клиента

- [x] Проверить, что `pytest` и `httpx` указаны в зависимостях проекта. Создать файл `backend/tests/test_health.py`.
- [x] Создать фикстуру Pytest `client`, которая возвращает `fastapi.testclient.TestClient(app)`. Допустимо использовать `httpx.AsyncClient` с `httpx.ASGITransport(app=app)`, если тесты будут асинхронными. В обоих вариантах клиент передаёт запросы приложению в памяти: не нужно запускать Uvicorn, Docker Compose или слушать порт `8000`.
  - [x] Фикстура должна создавать клиент перед тестом и корректно закрывать его после теста (`with TestClient(app) as client` или `yield`), чтобы выполнялись startup/shutdown-хуки приложения.
  - [x] Все unit-тесты health-check должны получать клиента через эту фикстуру, а не через `requests.get("http://127.0.0.1:8000/...")`.
- [x] Вынести проверку БД в отдельную точку подмены: например, dependency `check_database()` в обработчике маршрута или функцию сервиса, которую маршрут вызывает для `SELECT 1`. Она должна либо завершаться успешно, либо выбрасывать/возвращать контролируемую ошибку доступности БД.
  - [x] В тесте dependency заменить через `app.dependency_overrides[...]`, а функцию — через `monkeypatch`; после каждого теста override/patch не должен влиять на остальные тесты.
  - [x] Не открывать `Engine`, `Session` или соединение с PostgreSQL в момент импорта `main.py`: импорт приложения и создание тестового клиента должны работать без `DATABASE_URL`, запущенной БД и сетевого подключения. Engine допустимо создавать лениво при первом запросе либо в lifecycle-хуке приложения.
- [x] Отдельную тестовую БД применять только в интеграционных тестах, где действительно проверяются миграции Alembic, SQLAlchemy-модели, ограничения и реальные запросы.
  - [x] Использовать отдельный URL/контейнер и отдельное имя БД, например `apartment_test`; никогда не выполнять миграции, очистку таблиц или тестовые фикстуры в рабочей БД `apartment`.
  - [x] Ввести отдельную переменную окружения `TEST_DATABASE_URL`. Интеграционный код должен читать только её, а не обычный `DATABASE_URL`/набор `DB_*`: так запуск теста не сможет по ошибке получить строку подключения рабочего окружения.
  - [x] Перед созданием engine проверить URL: имя базы в нём должно оканчиваться на `_test` (например, `/apartment_test`). Если проверка не проходит, завершить тест с понятной ошибкой и не выполнять никаких SQL-команд.
  - [x] Такие тесты должны быть явно отделены маркером Pytest (например, `@pytest.mark.integration`) и запускаться отдельной командой или только при заданной `TEST_DATABASE_URL`.
  - [x] Зарегистрировать маркер в `pyproject.toml`, чтобы Pytest не выдавал предупреждение:

    ```toml
    [tool.pytest.ini_options]
    markers = [
      "integration: тест требует отдельную PostgreSQL test database",
    ]
    ```

  - [x] Обыстрые unit-тесты запускать командой `uv run pytest -m "not integration"`; интеграционные — только после явного задания URL: `TEST_DATABASE_URL='postgresql+psycopg://…/apartment_test' uv run pytest -m integration`. В CI передавать URL из сервиса PostgreSQL, а не из production secrets.
  - [x] Пример минимального интеграционного теста: применить миграции, открыть соединение с тестовой БД, выполнить `SELECT 1` и проверить, что возвращается `1`. Отдельными тестами затем проверить создание `Account`, уникальность `email` и другие ограничения модели.
  - [x] Для unit-тестов `GET /api/v1/health` реальная БД не нужна: подменить проверку успешным результатом или ошибкой подключения и проверить HTTP-ответ приложения.

### 5.2. Контракт health-check

- [x] Сохранить маршрут `GET /api/v1/health` публичным: он не требует JWT и не раскрывает пароль, URL БД или внутренние детали инфраструктуры.
- [x] При доступной БД вернуть `200 OK` и JSON:

  ```json
  {"status": "ok"}
  ```

- [x] Проверять БД лёгким запросом `SELECT 1` через SQLAlchemy, выполняя его в рамках обработки запроса.
- [x] Если подключение или запрос к БД завершается ошибкой, вернуть `503 Service Unavailable` и JSON:

  ```json
  {"status": "database_unavailable"}
  ```

- [x] Не возвращать наружу текст исключения PostgreSQL. Подробности допустимо записать в серверный лог без секретов.

### 5.3. Тесты health-check

- [x] Написать тест доступной БД: заменить dependency или функцию проверки на успешную, выполнить `GET /api/v1/health`, проверить статус `200` и точный JSON `{"status": "ok"}`.
- [x] Написать тест недоступной БД: заменить dependency или функцию проверки так, чтобы она имитировала ошибку подключения, выполнить тот же запрос и проверить `503` с JSON `{"status": "database_unavailable"}`.
- [x] Убедиться, что тест недоступной БД не обращается к настоящему PostgreSQL и не зависит от того, запущен ли Docker-контейнер.
- [x] При необходимости добавить отдельный интеграционный тест, который запускается только при наличии тестовой БД и проверяет реальный `SELECT 1`; не использовать для этого рабочую базу `apartment`.

Проверка: из корня репозитория выполнить `uv run pytest`. Pytest должен обнаружить тесты и завершиться с кодом `0`. Дополнительно вручную проверить запущенный API: при доступной БД `GET http://127.0.0.1:8000/api/v1/health` возвращает `200`, а при остановленной БД — `503`.

## 6. Docker Compose для приложения

- [x] Создать в корне репозитория `docker-compose.yml` или `docker-compose.yaml`. Compose объединяет несколько контейнеров в одно локальное окружение и автоматически создаёт для них общую сеть: сервис `api` сможет подключаться к PostgreSQL по имени хоста `apartment_db`, без `localhost` и без поиска IP-адреса контейнера.
- [x] Добавить сервис `apartment_db` на образе PostgreSQL, например `postgres:16-alpine`.
  - Задать `POSTGRES_DB=apartment`, `POSTGRES_USER` и `POSTGRES_PASSWORD` через `.env` или `environment`. Не коммитить реальные пароли: в репозиторий добавить только безопасный `.env.example`.
  - Сохранить данные в именованном Docker volume, например `postgres_data:/var/lib/postgresql/data`. Тогда обычные `docker compose down` и пересборка API не удалят данные БД; для полного сброса явно использовать `docker compose down -v`.
  - Не публиковать порт PostgreSQL наружу без необходимости. Для API достаточно внутренней сети Compose. Если нужен доступ с хоста для DBeaver/psql, добавить временный mapping `5432:5432` и помнить, что он может конфликтовать с локальным PostgreSQL.
- [x] Создать `backend/Dockerfile` для сервиса `api`.
  - Использовать базовый образ Python той же основной версии, что указана в `pyproject.toml` (сейчас 3.12+); выбрать slim-вариант, задать `WORKDIR /app`, скопировать `pyproject.toml` и `uv.lock`, установить зависимости через `uv sync --frozen`, затем скопировать код проекта.
  - Добавить `.dockerignore`: как минимум `.venv`, `.git`, `__pycache__`, `.env`, файлы IDE и frontend-зависимости. Это не даст случайно включить локальное окружение или секреты в образ и ускорит сборку.
  - Команда контейнера должна запускать Uvicorn: `uv run uvicorn backend.app.main:app --host 0.0.0.0 --port 8000`. Адрес `0.0.0.0` обязателен внутри контейнера, иначе опубликованный порт будет недоступен с хоста.
- [x] Добавить сервис `api`: сборка из `backend/Dockerfile`, переменные окружения, зависимость от `apartment_db`.
  - В `build` указать контекст корня репозитория (`context: .`), чтобы Python мог импортировать пакет `backend`; Dockerfile указать как `backend/Dockerfile`.
  - Передать настройки БД из `apartment_db`: `DB_HOST=apartment_db`, `DB_PORT=5432`, а также одинаковые `DB_NAME`, `DB_USER`, `DB_PASSWORD`. Внутри контейнера нельзя использовать `DB_HOST=localhost`: там это означало бы сам контейнер API, а не PostgreSQL.
  - Опубликовать API для браузера и ручной проверки: `ports: ["8000:8000"]`. Слева порт хоста, справа порт контейнера.
  - Добавить `depends_on` от `apartment_db` с условием `condition: service_healthy`. Это задерживает запуск API до готовности PostgreSQL, но не заменяет обработку ошибки БД в самом `/api/v1/health`.
- [x] Добавить health-check PostgreSQL и ожидание его готовности перед запуском API.
  - В сервисе `apartment_db` добавить `healthcheck` с `pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB`. Двойной знак `$` нужен, чтобы переменную раскрыл контейнер PostgreSQL, а не сам Docker Compose.
  - Задать разумные параметры, например `interval: 5s`, `timeout: 5s`, `retries: 10`, `start_period: 5s`. Статус смотреть командой `docker compose ps`; в норме у `apartment_db` будет `healthy`.
  - После запуска API его собственный маршрут `/api/v1/health` всё равно выполняет `SELECT 1`. Если БД упадёт уже после старта Compose, маршрут должен вернуть `503`, а не ложный `200`.
- [x] Настроить порт API `8000` для разработки.
  - Проверить, что `http://localhost:8000/docs` открывает Swagger UI API.
  - Если порт уже занят, изменить только левую часть mapping, например `8001:8000`; адрес API на хосте тогда будет `http://localhost:8001`.
- [ ] Проверить запуск чистого окружения только для `apartment_db` и `api` командой `docker compose up --build`.
  - Перед первой проверкой создать `.env` по примеру `.env.example` и заполнить только локальные значения. Не использовать production-пароль.
  - Запустить в фоне: `docker compose up --build -d`; проверить состояние `docker compose ps` и логи API `docker compose logs api --follow`. Остановить просмотр логов — `Ctrl+C`, контейнеры при этом продолжат работать.
  - Проверить API: `curl -i http://localhost:8000/api/v1/health`. Ожидаются `HTTP/1.1 200 OK` и `{"status":"ok"}`. Для остановки окружения использовать `docker compose down`; данные volume сохранятся.
  - Для воспроизводимой проверки «с чистого листа» остановить окружение и удалить только проектные volume: `docker compose down -v`, затем снова `docker compose up --build`. Эта команда удаляет данные PostgreSQL, поэтому не применять её к окружению с важными данными.

Проверка: запрос к `http://localhost:8000/api/v1/health` возвращает успешный статус.

### 6.1. Отдельная БД для интеграционных тестов

Этот блок выполнять только после запуска основного Compose-окружения из раздела 6. Unit-тесты health-check от него не зависят и продолжают работать без PostgreSQL.

- [ ] Поднять отдельную БД для интеграционных тестов: отдельным Compose-сервисом либо отдельной базой PostgreSQL. Задать `TEST_DATABASE_URL`, например `postgresql+psycopg://postgres:postgres@127.0.0.1:5433/apartment_test`. Порт `5433` приведён только как пример: он помогает не перепутать тестовый сервис с основной БД на `5432`.
- [ ] Выполнить `uv run pytest -m integration` с заданной `TEST_DATABASE_URL`. В начале набора применить миграции Alembic к `apartment_test`, а после тестов удалить созданные записи или пересоздать схему. Обе операции допустимы только после проверки URL с суффиксом `_test`.

## 7. Web-клиент

- [ ] Создать Vite-проект в `frontend` с React и TypeScript.
- [ ] Удалить демонстрационный экран и вывести временную страницу Renovation Planner.
- [ ] Добавить переменную `VITE_API_URL` и небольшой клиент для запроса health-check.
- [ ] Показать на странице статус доступности API.

Проверка: открыть `http://localhost:5173` и увидеть статус API.

### 7.1. Подключить готовый web-клиент к Docker Compose

- [ ] Создать `frontend/Dockerfile` для разработки только после появления Vite-проекта.
- [ ] Добавить сервис `web` в `docker-compose.yaml`: собрать его из `frontend/Dockerfile`, пробросить порт `5173:5173` и передать `VITE_API_URL=http://localhost:8000`. Это адрес API для браузера на хосте; имя `api` используют только контейнеры при обращении друг к другу.
- [ ] Проверить `http://localhost:5173`: Vite-клиент должен открыться и показать статус API.
- [ ] Проверить полный чистый запуск после добавления `web`: `docker compose down -v`, затем `docker compose up --build -d`; все сервисы `apartment_db`, `api` и `web` должны быть запущены. Команда `down -v` удаляет данные PostgreSQL, поэтому применять её только к локальному окружению без важных данных.

## 8. CI

- [ ] Создать `.github/workflows/ci.yml`.
- [ ] Запускать backend-тесты и проверку импорта приложения.
- [ ] Запускать проверку сборки frontend.
- [ ] Использовать сервис PostgreSQL в CI, если тесты требуют реальную БД.
- [ ] Добавить статус CI в README после появления удалённого репозитория GitHub.

Проверка: workflow проходит после push или pull request.

## Критерии завершения 1.2

- [ ] `docker compose up --build` поднимает `apartment_db`, `api` и `web`.
- [x] `/api/v1/health` проверяет API и БД.
- [x] Первая миграция воспроизводимо создаёт таблицу аккаунтов.
- [ ] Есть тест health-check и он проходит локально и в CI.
- [x] Документированы API-префикс, авторизация и правило доступа.
- [ ] Web-клиент обращается к API.

## Текущий шаг

Создать Web-клиент в разделе 7, затем добавить сервис `web` в Docker Compose. После этого настроить CI из раздела 8.
