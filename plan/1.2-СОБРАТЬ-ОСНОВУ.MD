# 1.2. Собрать основу — подробный план

Этот документ детализирует пункт 1.2 из [основного плана](PLAN.MD). Он рассчитан на обучение: выполняем одну небольшую задачу, проверяем результат и только затем переходим дальше.

## Результат этапа

В репозитории работают API, web-клиент и PostgreSQL. Проект поднимается одной командой Docker Compose, изменения схемы БД проходят через миграции, health-check и тесты выполняются локально и в CI.

## Принципы работы

- Не пишем функциональность проектов и покупок до готовности основы.
- Каждый шаг должен иметь понятный результат и команду проверки.
- Секреты не храним в Git: используем `.env` и `.env.example`.
- Начинаем с минимальной реализации; усложняем, когда появляется практическая потребность.
- Все команды `uv` выполняем из корня репозитория, где находятся `pyproject.toml` и `uv.lock`.

## 1. Каркас и локальный запуск API

- [x] Создать папки `backend/app`, `backend/tests`, `frontend` и `plan`.
- [x] Инициализировать Python-проект через корневой `pyproject.toml`.
- [x] Добавить FastAPI, Uvicorn, SQLAlchemy, Alembic, Psycopg, Pydantic Settings, Pytest и HTTPX.
- [x] Создать `backend/app/main.py` с приложением FastAPI.
- [x] Реализовать `GET /api/v1/health`, временно без проверки БД, с ответом `{"status": "ok"}`.

Проверка: из корня репозитория выполнить `uv run uvicorn backend.app.main:app --reload`, затем открыть `http://127.0.0.1:8000/docs`.

## 2. PostgreSQL и конфигурация

- [x] Создать корневой файл Docker Compose (`docker-compose.yml` или `docker-compose.yaml`) с сервисом PostgreSQL.
- [x] Создать `.env.example` с именем БД, пользователем, паролем и портами без реальных секретов.
- [x] Добавить `backend/app/core/config.py`: настройки читаются из переменных окружения.
- [ ] Настроить строку подключения `DATABASE_URL` для локального запуска и Docker.
- [x] Убедиться, что API получает настройки, но пока не обязан обращаться к БД на каждом запросе.

Проверка: `docker compose up -d <имя-сервиса-БД>` и подключение через `docker compose exec <имя-сервиса-БД> psql -U <пользователь> -d <база>`.

## 3. SQLAlchemy и миграции

В этом разделе появятся три связанных слоя:

```text
модель Account в Python → SQLAlchemy metadata → миграция Alembic → таблица accounts в PostgreSQL
```

SQLAlchemy описывает таблицы классами Python и создаёт подключения к БД. Alembic хранит историю изменений схемы: вместо ручного создания таблиц каждый разработчик применяет одинаковые версии миграций.

### 3.1. Подключение SQLAlchemy

- [x] Создать пакет `backend/app/db` и файл `base.py`.
- [x] В `base.py` создать класс `Base`, наследующийся от `DeclarativeBase`. Все модели будут наследоваться от него.
- [x] Создать модуль подключения, например `backend/app/db/session.py`.
- [x] В нём создать синхронный `Engine` через `create_engine(settings.database_url)`: берём готовый URL из `core/config.py`, не дублируем пароль и хост.
- [x] Создать фабрику сессий `SessionLocal` через `sessionmaker`. Сессия будет единицей работы с БД внутри будущих API-запросов.
- [x] Не создавать соединение при импорте модуля: `create_engine()` только настраивает Engine; настоящее соединение откроется при первом запросе к БД.

На этом этапе используем синхронный SQLAlchemy: у нас драйвер `psycopg`, а первые endpoint'ы пока обычные `def`. Асинхронный вариант намеренно не добавляем, чтобы не смешивать два подхода.

Проверка: маленький временный Python-скрипт или однострочная команда импортирует Engine и выполняет `SELECT 1`. Результат должен быть `1`.

### 3.2. Подготовка Alembic

- [x] Из корня репозитория выполнить `uv run alembic init alembic`. Появятся папка `alembic/`, файл `alembic.ini` и папка `alembic/versions/` для будущих миграций.
- [x] Оставить в `alembic.ini` технические настройки Alembic, но не хранить там адрес БД и пароль.
- [x] В `alembic/env.py` импортировать `settings` из `backend.app.core.config` и передать `settings.database_url` в конфигурацию Alembic программно.
- [x] В `alembic/env.py` импортировать `Base` и присвоить `target_metadata = Base.metadata`. После создания `Account` в разделе 3.3 отдельно импортировать модель в `env.py`, иначе Alembic не узнает о таблице и автогенерация будет пустой.

Проверка: команда `uv run alembic current` запускается без ошибки и использует БД из `.env`.

### 3.3. Первая модель

- [x] Создать пакет `backend/app/models` и файл `account.py`.
- [x] Создать класс `Account(Base)` и задать имя таблицы `accounts` через `__tablename__`.
- [x] Добавить поле `id`: UUID, первичный ключ, значение создаётся автоматически.
- [x] Добавить поле `email`: строка, обязательное значение, уникальный индекс. В базе не должно быть двух аккаунтов с одинаковым email.
- [x] Добавить поле `password_hash`: обязательная строка. Храним только хеш пароля, никогда не исходный пароль.
- [x] Добавить поле `created_at`: дата и время создания записи с часовым поясом; значение по умолчанию задаёт сама БД.
- [x] Проверить, что `alembic/env.py` импортирует `Account`, иначе модель не попадёт в `Base.metadata`.

На этом шаге не добавляем регистрацию, JWT или проверку пароля — только описание будущей таблицы.

### 3.4. Первая миграция

- [ ] Создать миграцию командой `uv run alembic revision --autogenerate -m "create accounts table"`.
- [ ] Открыть созданный файл в `alembic/versions/` и прочитать функции `upgrade()` и `downgrade()`. Автогенерацию всегда проверяем до применения: это подсказка Alembic, а не безусловно правильный SQL.
- [ ] Применить миграцию командой `uv run alembic upgrade head`.
- [ ] Убедиться, что появилась таблица `accounts` и служебная таблица `alembic_version`.
- [ ] Проверить историю командой `uv run alembic current` или `uv run alembic history`.

Проверка в PostgreSQL: `docker compose exec <имя-сервиса-БД> psql -U <пользователь> -d <база>`, затем команда `\\dt` покажет список таблиц.

## Критерии завершения раздела 3

- [ ] `settings.database_url` используется и приложением, и Alembic; URL не копируется в несколько файлов.
- [ ] В Git добавлены `alembic/`, `alembic.ini` и первая миграция, но не добавляется `.env`.
- [ ] Чистая БД после `uv run alembic upgrade head` содержит таблицы `accounts` и `alembic_version`.

## 4. Контракт API и доступ

- [ ] Описать соглашения API в `plan/API.MD`: префикс `/api/v1`, JSON, форматы ошибок, пагинация и даты в ISO 8601.
- [ ] Зафиксировать ресурсы MVP: `auth`, `projects`, `rooms`, `purchases`.
- [ ] Зафиксировать, какие операции требуют входа.
- [ ] Описать правило доступа: пользователь видит и меняет только собственные проекты; приглашения и роли — после MVP.
- [ ] Выбрать механизм: JWT access token в заголовке `Authorization: Bearer <token>`; refresh token добавим лишь при реализации входа, если понадобится.

Результат: документ согласован до написания эндпоинтов сценария 1.3.

## 5. Тесты и настоящий health-check

- [ ] Настроить Pytest и тестовый клиент FastAPI.
- [ ] Написать тест, проверяющий ответ `GET /api/v1/health`.
- [ ] Добавить проверку доступности PostgreSQL в health-check.
- [ ] Разделить статусы: приложение живо — `200`, БД недоступна — `503`.
- [ ] Добавить тесты обоих случаев, используя отдельную тестовую БД или подмену подключения.

Проверка: из корня репозитория выполнить `uv run pytest`.

## 6. Docker Compose для приложения

- [ ] Добавить сервис `api`: сборка из `backend/Dockerfile`, переменные окружения, зависимость от `db`.
- [ ] Добавить health-check PostgreSQL и ожидание его готовности перед запуском API.
- [ ] Добавить сервис `web` после создания web-клиента.
- [ ] Настроить порты: API — `8000`, web — `5173` для разработки.
- [ ] Проверить запуск чистого окружения командой `docker compose up --build`.

Проверка: запрос к `http://localhost:8000/api/v1/health` возвращает успешный статус.

## 7. Web-клиент

- [ ] Создать Vite-проект в `frontend` с React и TypeScript.
- [ ] Удалить демонстрационный экран и вывести временную страницу Renovation Planner.
- [ ] Добавить переменную `VITE_API_URL` и небольшой клиент для запроса health-check.
- [ ] Показать на странице статус доступности API.
- [ ] Добавить Dockerfile для разработки web-клиента и подключить сервис `web` к Compose.

Проверка: открыть `http://localhost:5173` и увидеть статус API.

## 8. CI

- [ ] Создать `.github/workflows/ci.yml`.
- [ ] Запускать backend-тесты и проверку импорта приложения.
- [ ] Запускать проверку сборки frontend.
- [ ] Использовать сервис PostgreSQL в CI, если тесты требуют реальную БД.
- [ ] Добавить статус CI в README после появления удалённого репозитория GitHub.

Проверка: workflow проходит после push или pull request.

## Критерии завершения 1.2

- [ ] `docker compose up --build` поднимает `db`, `api` и `web`.
- [ ] `/api/v1/health` проверяет API и БД.
- [ ] Первая миграция воспроизводимо создаёт таблицу аккаунтов.
- [ ] Есть тест health-check и он проходит локально и в CI.
- [ ] Документированы API-префикс, авторизация и правило доступа.
- [ ] Web-клиент обращается к API.

## Текущий шаг

Начинаем с раздела 1: создаём минимальный FastAPI-проект и временный health-check без БД. После этого разберём твою реализацию и перейдём к Docker/PostgreSQL.
